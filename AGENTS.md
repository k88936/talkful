# DO

- to make workspace , ALWAYS split code into small module.
- (frontend) Always prefer Ring UI components over custom implementations.
- (frontend) ringui component use example is in `ringui-docs`, read as you need.

# DONT DO

- pile all rubbish in single file
- DONT do any fallback, just crash
- (frontend) Never use .css file, use tailwindcss inline instead
- (frontend) never config color in css
- (frontend) Never use div/span/... use ringui's Group , Island ... instead as container

